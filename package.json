{
  "license": "MIT",
  "dependencies": {
    "virtual-dom": "^2.1.1"
  },
  "devDependencies": {
    "json-server": "^0.15.0"
  },
  "scripts": {
    "serve": "json-server --watch db.json",
    "watch": "sh ./build-watch",
    "build": "pulp browserify -O > app.js"
  }
}
